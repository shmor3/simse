projects:
  - 'simse-*'
  - 'simse-code/*'

vcs:
  hooks:
    pre-commit:
      - 'export PATH="$HOME/.proto/shims:$HOME/.proto/bin:$PATH" && moon run :lint :format-check :test --affected --status=staged'
  sync: true
