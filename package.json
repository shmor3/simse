{
	"name": "simse",
	"private": true,
	"scripts": {
		"build:vector-engine": "cd simse-vector && cargo build --release",
		"build:vfs-engine": "cd simse-vfs && cargo build --release",
		"build:acp-engine": "cd simse-acp && cargo build --release",
		"build:mcp-engine": "cd simse-mcp && cargo build --release",
		"build:core": "cd simse-core && cargo build --release",
		"build:tui": "cd simse-tui && cargo build --release",
		"build:sandbox-engine": "cd simse-sandbox && cargo build --release"
	}
}
