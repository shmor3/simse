# simse-api

API gateway Cloudflare Worker. Proxies requests to backend services (auth, payments, analytics) with auth validation and secrets middleware.

## Development

```bash
npm run dev
```

## Lint

```bash
npm run lint
```
