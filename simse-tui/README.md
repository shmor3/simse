# simse-tui

Terminal UI built with ratatui and crossterm, following the Elm Architecture (Model/Update/View). Provides markdown rendering, tool call display, autocomplete, permission dialogs, and overlay panels.

## Build

```bash
cargo build --release
```

## Test

```bash
cargo test
```
