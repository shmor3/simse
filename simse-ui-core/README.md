# simse-ui-core

Platform-agnostic UI logic with no I/O or async dependencies. Provides conversation state management, permission handling, keybinding registry, tool call parsing, and command registry (34 commands).

## Build

```bash
cargo build --release
```

## Test

```bash
cargo test
```
